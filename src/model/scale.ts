
type scaleInterval = 'tone' | 'semitone';

let majorScale : scaleInterval[] = ['tone', 'tone', 'semitone', 'tone', 'tone', 'tone', 'semitone'];
let minorScale : scaleInterval[] = ['tone', 'semitone', 'tone', 'tone', 'tone', 'semitone', 'tone']

export {
    majorScale, 
    minorScale,
}