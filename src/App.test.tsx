// import React from 'react';
// import { render, screen } from '@testing-library/react';
// import '@testing-library/jest-dom/extend-expect'; // for the latest jest-dom matchers
// import App from './App';

// // Test case: Check if the 'learn react' link is rendered in the App component
// test('renders learn react link', () => {
//   // Render the App component
//   render(<App />);

//   // Find the element that contains 'learn react'
//   const linkElement = screen.getByText(/learn react/i);

//   // Assert that the link element is in the document
//   expect(linkElement).toBeInTheDocument();
// });